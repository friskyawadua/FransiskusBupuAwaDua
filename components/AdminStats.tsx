import { AdminAnalytics } from '@/types/product';
export default function AdminStats({data}:{data:AdminAnalytics}){return <div className='grid md:grid-cols-3 gap-4'>{[['Total Searches',data.totalSearches],['Total Clicks',data.totalClicks],['CTR',`${data.ctr.toFixed(2)}%`],['Estimated GMV',`Rp${data.estimatedGmv}`],['Estimated Commission',`Rp${data.estimatedCommission}`],['Top Marketplace',Object.entries(data.marketplaceClicks).sort((a,b)=>b[1]-a[1])[0][0]]].map(([k,v])=><div key={String(k)} className='bg-white border rounded p-4'><p className='text-xs text-gray-500'>{k}</p><p className='font-bold'>{v}</p></div>)}</div>}
