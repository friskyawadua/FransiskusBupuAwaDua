import ProductCard from '@/components/ProductCard'; import { parseQuery } from '@/lib/queryParser'; import { getRecommendations } from '@/lib/recommendationEngine';
export default function Results({searchParams}:{searchParams:{q?:string}}){const q=searchParams.q||''; const parsed=parseQuery(q); const recs=getRecommendations(parsed); return <main className='max-w-6xl mx-auto p-4'><h2 className='font-bold text-2xl'>Hasil Rekomendasi</h2><p className='text-sm text-gray-600'>Query: {q} | Bahasa: {parsed.language}</p><div className='grid md:grid-cols-2 lg:grid-cols-3 gap-4 mt-4'>{recs.map(r=><ProductCard key={r.product.id} item={r} lang={parsed.language==='en'?'en':'id'} />)}</div></main>}
